# Domains for SSL termination (Envoy will decrypt HTTPS and forward HTTP to K8s)
# Comma-separated list
# Example: DOMAINS_SSL_TERMINATION=whoami.ope.apps.technovateit-solutions.com,api.ope.apps.technovateit-solutions.com
DOMAINS_SSL_TERMINATION=whoami.ope.apps.technovateit-solutions.com

# Domains for SSL passthrough (Envoy will forward HTTPS traffic as-is to K8s)
# Comma-separated list
# Example: DOMAINS_SSL_PASSTHROUGH=dashboard.k8s.ope.infra.technovateit-solutions.com
DOMAINS_SSL_PASSTHROUGH=dashboard.k8s.ope.infra.technovateit-solutions.com

# Admin email for Let's Encrypt notifications
LETSENCRYPT_EMAIL=admin@technovateit-solutions.com

# Kubernetes cluster IP where traffic will be forwarded
K8S_CLUSTER_IP=192.168.1.200

# GCP Service Account credentials file path (relative to docker-compose.yml or absolute path)
GCP_CREDENTIALS_PATH=./gcp-credentials.json

# GCP credentials container path (path inside the certbot container)
GCP_CREDENTIALS_CONTAINER_PATH=/gcp-credentials.json
