# Domains for SSL termination to K8s (Envoy will decrypt HTTPS and forward HTTP to K8s)
# Comma-separated list
# Example: DOMAINS_K8S_SSL_TERMINATION=whoami.ope.apps.technovateit-solutions.com,api.ope.apps.technovateit-solutions.com
DOMAINS_K8S_SSL_TERMINATION=whoami.ope.apps.technovateit-solutions.com

# Domains for SSL termination to Nomad (Envoy will decrypt HTTPS and forward HTTP to Nomad)
# Comma-separated list
# Example: DOMAINS_NOMAD_SSL_TERMINATION=whoami-nomad.ope.apps.technovateit-solutions.com,api-nomad.ope.apps.technovateit-solutions.com
DOMAINS_NOMAD_SSL_TERMINATION=

# Domains for SSL passthrough to K8s (Envoy will forward HTTPS traffic as-is to K8s)
# Comma-separated list
# Example: DOMAINS_K8S_SSL_PASSTHROUGH=dashboard.k8s.ope.infra.technovateit-solutions.com
DOMAINS_K8S_SSL_PASSTHROUGH=

# Domains for SSL passthrough to Nomad (Envoy will forward HTTPS traffic as-is to Nomad)
# Comma-separated list
# Example: DOMAINS_NOMAD_SSL_PASSTHROUGH=
DOMAINS_NOMAD_SSL_PASSTHROUGH=

# Admin email for Let's Encrypt notifications
LETSENCRYPT_EMAIL=admin@technovateit-solutions.com

# Kubernetes cluster IP where traffic will be forwarded
K8S_CLUSTER_IP=192.168.1.200

# Nomad cluster IP where traffic will be forwarded (Traefik or direct service)
NOMAD_CLUSTER_IP=192.168.1.201
NOMAD_CLUSTER_PORT=8080

# GCP Service Account credentials file path (relative to docker-compose.yml or absolute path)
GCP_CREDENTIALS_PATH=./gcp-credentials.json

# GCP credentials container path (path inside the certbot container)
GCP_CREDENTIALS_CONTAINER_PATH=/gcp-credentials.json

# GCP Project ID (for reference, not used directly by docker-compose)
# GCP_PROJECT_ID=your-gcp-project-id
